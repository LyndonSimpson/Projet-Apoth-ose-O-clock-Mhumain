//todo middleWare qui met dans les "res.locals" le id du profil chat connecté ou du profil humain
//todo - il faut créer deux routes pour cat et human (login cat, logout cat // login human, logout human). dans les controlleurs respectifs (signuptLoginCat / signUpLoginHuman)

//Dans ces deux controlleurs on va suivre la même chose que signup action pour user mais sans demander de mdp pour juste stocker dans les locals le id avec une requête ou en utilisant les locals (tout stocker dans locals).